/* eslint-disable no-unused-vars */
import React from 'react';
/* eslint-enable no-unused-vars */

import ReactDOM from 'react-dom';
import ScrollyExample from './components/ScrollyExamples';

ReactDOM.render(
    <div>
        <ScrollyExample label="Scrolly" />
    </div>,
    document.getElementById('scrolly-frame')
);

