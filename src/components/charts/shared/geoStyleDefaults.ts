import type { Style } from "../../stream/types"

/** Shared background-land style for geo charts that layer marks over areas. */
export const GEO_BACKGROUND_AREA_STYLE: Style = {
  fill: "#f0f0f0",
  stroke: "#ccc",
  strokeWidth: 0.5,
}
